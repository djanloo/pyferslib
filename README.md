# pyferslib

Python bindings for CAEN's FERSlib

## Building on Windows (CMake)

```
cmake --preset default
cmake --build --preset default
```

## Building on Linux (Meson)

```
meson setup builddir
meson compile -C builddir
```
